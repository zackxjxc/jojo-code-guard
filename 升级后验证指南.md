# jojo-code-guard 0.2.16 升级与验证指南

本手册供 AI 在用户明确要求“执行本手册”时自动执行。它验证 0.2.16 的按需 Skill 发现、低开销 Hook
和编码/diff 守护是否实际生效。

## 授权边界

- 用户只要求阅读、解释或预览本手册时，只执行只读检查并报告结果。
- 用户明确要求执行本手册时，允许升级 marketplace、重新安装插件，以及精确迁移本手册列出的旧版 jojo
  用户配置。
- 迁移前先读取目标、记录现有内容和文件属性；只删除明确属于 jojo 的旧节或旧 Hook，不覆盖同文件中的
  其他用户规则。
- 不能确认某段内容或某个 Hook 是否属于 jojo 时，停止该目标的修改并报告，不猜测。

## 1. 升级插件

在 PowerShell 或终端中执行：

```text
codex plugin marketplace upgrade jojo-code-guard
codex plugin add jojo-code-guard@jojo-code-guard
codex plugin list
```

验收：列表中 `jojo-code-guard` 的版本为 `0.2.16`。升级完成后必须新开 Codex 任务；已打开的任务仍可能
保留旧 Skill 和旧 Hook 上下文。

## 2. 迁移旧版重复加载配置

在新任务中运行：

```text
使用 jojo-code-guard-doctor 检查当前仓库和旧版重复加载配置。
```

0.2.x 遗留来源可能包括：

- `C:\Users\ZACK\.codex\AGENTS.md` 中的 `## jojo-code-guard 自动加载` 节；
- `C:\Users\ZACK\.codex\hooks.json` 中指向 `hooks/session-start` 的 `SessionStart` 配置；
- `C:\Users\ZACK\.codex\hooks\session-start`；
- Claude 的 `C:\Users\ZACK\.claude\CLAUDE.md`、`settings.json` 和 `hooks\session-start` 中的同类来源。

doctor 报告 `ACTION_REQUIRED` 后，先备份每个即将修改的用户文件。只移除以下内容：

1. 以 `## jojo-code-guard 自动加载` 开头、到下一个同级或更高标题之前的完整旧节；
2. 明确执行旧 jojo `session-start` 的 `SessionStart` Hook 处理器；
3. 内容包含 `jojo-code-guard` 或 `JOJO_CODE_GUARD` 的旧 `session-start` 脚本。

不要删除其他全局规则、其他插件 Hook、未知脚本或用户自定义内容。迁移后再次运行 doctor，预期不再出现
“旧版自动加载节”“旧版手工 SessionStart”或“旧版手工 SessionStart 配置”。

## 3. 审阅当前 Hook

在 Codex 使用 `/hooks` 审阅并信任新版本 Hook。jojo 应只包含：

- `SessionStart`，且 matcher 为 `compact`
- `PreToolUse`
- `PostToolUse`
- `Stop`

`SessionStart` 只能用于压缩恢复，不能匹配 `startup`、`resume` 或 `clear`，也不能读取或注入 Skill 正文。
不应存在 jojo 的 `UserPromptSubmit`。若用户级旧 Hook 仍在，先完成第 2 步再继续。

## 4. 运行确定性回归验证

在插件仓库根目录执行：

```text
python -B -m unittest tests.test_claude_adapter
python -B -m unittest discover -s tests
python -B skills/jojo-code-guard/scripts/check_diff.py --repo . --tracked-revision HEAD
```

验收：测试通过，`check_diff.py` 返回 `OK`，且没有编码、BOM、换行或 Git 空白阻断项。

`tests.test_claude_adapter` 覆盖以下关键不变量：

- 纯聊天 Stop 没有状态时不访问 Git；
- `git status`、`git diff`、`rg`、`Get-Content` 等已确认只读命令不访问 Git；
- 真正写入只运行一次 PostToolUse 完整检查；
- Stop 不复制 `last_assistant_message`；
- 并发工具和跨仓库状态不会互相覆盖；
- 早期未解决阻断项不能被后续成功工具掩盖；
- Hook 清单只有 `SessionStart: compact`、PreToolUse、PostToolUse、Stop，且没有 UserPromptSubmit。

## 5. 新会话行为烟测

在完成第 1 至第 3 步后新开一个 Codex 任务，依次验证：

1. 纯聊天：询问一个与文件无关的问题。预期没有 jojo Git/diff 诊断或加载提示。
2. 只读命令：执行 `git status --short` 或 `rg --files`。预期没有 jojo Hook 输出，也不触发完整 diff 检查。
3. 真实写入：在可丢弃的 Git 测试仓库中只修改一行文本。预期只有工作区真实变化后才触发一次完整检查；
   随后结束回合不重复输出相同诊断。

无法从普通聊天界面直接读取模型内部 token 数。以第 3、4、5 步的 Hook 清单、零输出行为和自动化测试作为
可重复验证证据。

## 完成标准

全部满足以下条件才报告完成：

- 已安装版本为 `0.2.16`；
- 新开任务；
- 旧版 jojo 自动加载节和手工 SessionStart 已迁移或确认不存在；
- `/hooks` 中有且只有新的 jojo `SessionStart: compact`、PreToolUse、PostToolUse、Stop；
- 回归测试和提交树守护通过；
- 纯聊天、只读命令、真实写入三类烟测均符合第 5 步预期。
