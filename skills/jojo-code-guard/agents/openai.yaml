interface:
  display_name: "jojo-code-guard"
  short_description: "保护已有文件的编码、换行与最小 diff"
  default_prompt: "使用 $jojo-code-guard 检查本次文件修改是否保留编码、BOM、换行和最小 diff。"
policy:
  allow_implicit_invocation: true
