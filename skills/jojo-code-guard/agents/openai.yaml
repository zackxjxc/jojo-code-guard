interface:
  display_name: "jojo-code-guard"
  short_description: "按任务守护编码、换行和最小 diff，纯对话不读取项目"
  default_prompt: "使用 $jojo-code-guard 先执行消息级检查，再只加载本轮实际需要的文件、C++、PowerShell 或 Git 守护规则。"
policy:
  allow_implicit_invocation: true
